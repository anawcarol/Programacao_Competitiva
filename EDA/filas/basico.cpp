#include <bits/stdc++.h>

using namespace std;

int main(){


//operações básicas filas:
queue<int> fila;

fila.push(10);  // Insere no final
fila.pop();     // Remove do início (não retorna valor!)
fila.front();   // Acessa elemento do início
fila.back();    // Acessa elemento do final  
fila.empty();   // Verifica se está vazia
fila.size();    // Retorna número de elementos

}


#include <bits/stdc++.h>
using namespace std;

int main()
{
    int m, n, l, k;

    while (m > 0 && n > 0)
    {
        
        cin >> m >> n >> l >> k;
        vector<int> x;

        for (int i = 0; i < k; i++)
        {
            int a;
            cin >> a;
            x.push_back(a);
        }
    }

    return 0;
}